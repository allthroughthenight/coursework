Project 1:
A C programs thats the culmination of hw1, 3, and 5. The program takes user input for record structs, stores them using a linked-list on the heap, and write it out to a file when exited. Or imports a file when started.
